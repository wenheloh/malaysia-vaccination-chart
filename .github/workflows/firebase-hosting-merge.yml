# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12]

    steps:
      - uses: actions/checkout@v2
      - name: install firebase
        run: npm i firebase-tools -g
      - name: Inject environmental variables
        run: firebase functions:config:set ssrapp.api_base_url="https://malaysia-vaccination-chart.web.app/api" ssrapp.google_map_api_key="${{ secrets.GOOGLE_MAP_API_KEY }}" --token ${{ secrets.GCLOUD_TOKEN }}
      - name: Clean up any previous build files
        run: rm -rf node_modules && rm -rf .nuxt && npm cache clean --force
      - name: Prepare artifact
        run: npm ci && npm run test && npm run build
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: deploy
        run: firebase deploy --token ${{ secrets.GCLOUD_TOKEN }}
